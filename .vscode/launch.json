{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Launch Worker",
      "type": "go",
      "request": "launch",
      "mode": "debug",
      "program": "${workspaceFolder}/server/cmd/api",
      "envFile": "${workspaceFolder}/.env"
    }
  ]
}
